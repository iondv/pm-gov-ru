Вы назначены заместителем <%=message.employee%> на период с <%=message.start && message.start.toLocaleString("ru")%> по <%=message.end && message.end.toLocaleString("ru")%>.

Это письмо было создано и отправлено автоматически и является оповещение.
Не нужно отвечать на это письмо.